\section{Discussion}

The results should be framed as an explanation of when DFL has room to improve,
not as a universal negative result about packing problems. Close substitutes can
make two-stage predictions decision-robust even when some edge predictions are
poor. Conversely, coupled path-like decisions can amplify a small ranking error
into large regret.

The next writing step is to replace the placeholder experiment paragraph with
the exact decision-analysis tables and figure references, then tighten the
claims around the strongest verified evidence: observed near ties, solution
overlap, and symmetric-difference error correlations.
